import os
import sys

windowWi = 860+67 # Map width + Menu width
windowHi = 500    # An arbitrary height

mapWi = 43 # Height and Width
mapHi = 25 # in terms of tiles

mainPath = os.path.dirname(os.path.realpath(sys.argv[0]))